# CacheConRedis

Este proyecto tiene como fin utilizar redis como segundo server de consulta a partir de la primera interacción que el cliente tiene con la api.

Como redis almacena la información en RAM, tiene velocidades que un SSD no puede alcanzar, por lo tanto devuelve tiempos de respuesta rápidos.
